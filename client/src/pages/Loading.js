import React from 'react';
// parse window here into base + query parameters

const Loading = () => {
  // parse window here into base + query parameters

  return <>Loading...</>;
};

export default Loading;
